{
  "_readme": "PostgreSQL MCP server — allows Claude to query a Postgres database directly. Ideal for data exploration, debugging query results, and schema inspection.",
  "_setup": [
    "1. Set your database connection string as an env var:",
    "   export PG_CONNECTION_STRING=postgresql://user:password@localhost:5432/mydb",
    "2. Recommended: use a read-only database user to prevent accidental writes",
    "3. Paste the 'mcpServers' block into your .claude/settings.json"
  ],
  "_security": "Use a read-only Postgres user wherever possible. Never expose production credentials — use a local or staging database.",
  "mcpServers": {
    "postgres": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-postgres"],
      "env": {
        "POSTGRES_CONNECTION_STRING": "$PG_CONNECTION_STRING"
      }
    }
  }
}
