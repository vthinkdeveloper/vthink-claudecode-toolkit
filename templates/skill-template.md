---
name: skill-name
description: What this skill does. Use when <trigger conditions — be specific about phrases or situations that should invoke it>.
---

## Overview

[What workflow or domain knowledge this skill encodes, and why it matters.]

## When to Use

[Specific trigger conditions. E.g., "Use when the user asks to generate a REST endpoint" or "triggers on /command-name".]

## Process

[Step-by-step instructions Claude should follow. Be explicit about:
- What context to gather first (read files, inspect project structure, etc.)
- Step-by-step behavior
- Output format or files to create/edit
- How to handle ambiguity (ask the user vs. make a reasonable default)]

## Configuration

[If the skill reads config from a local file (e.g., package.json), describe:
- What file it looks for
- What fields it reads
- What it does if the file is missing]

## Verification

[How to confirm the skill was applied correctly — what should be true when it completes.]
